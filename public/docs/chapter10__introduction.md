# 10.1. Introdução

É hora de tornar o sistema LFS inicializável. Este capítulo discute a criação do arquivo /etc/fstab, a construção de um kernel para o novo sistema LFS e a instalação do boot loader GRUB para que o sistema LFS possa ser selecionado para inicialização na inicialização.
